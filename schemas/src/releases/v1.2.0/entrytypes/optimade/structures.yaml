$$schema: "https://schemas.optimade.org/meta/v1.2/optimade/entrytype_definition"
$id: "https://schemas.optimade.org/defs/v1.2/entrytypes/optimade/structures"
title: "Structures"
description: "The structures entry type describes a crystal structure via its unit cell"
x-optimade-definition:
  kind: "entrytype"
  format: "1.2"
  version: "1.2.0"
  name: "structures"
type: object
properties:
  id:
    $$inherit: "/properties/optimade/structures/id"
    x-optimade-requirements:
      support: "must"
      sortable: false
      query-support: "all mandatory"
      response-level: "always"

  type:
    $$inherit: "/properties/optimade/structures/type"
    x-optimade-requirements:
      support: "must"
      sortable: false
      query-support: "all mandatory"
      response-level: "always"

  immutable_id:
    $$inherit: "/properties/optimade/structures/immutable_id"
    x-optimade-requirements:
      support: "may"
      sortable: false
      query-support: "all mandatory"
      response-level: "may"

  last_modified:
    $$inherit: "/properties/optimade/structures/last_modified"
    x-optimade-requirements:
      support: "should"
      sortable: false
      query-support: "all mandatory"
      response-level: "must"

  elements:
    $$inherit: "/properties/optimade/structures/elements"
    x-optimade-requirements:
      support: "should"
      sortable: false
      query-support: "all mandatory"
      response-level: "may"

  nelements:
    $$inherit: "/properties/optimade/structures/nelements"
    x-optimade-requirements:
      support: "should"
      sortable: false
      query-support: "all mandatory"
      response-level: "may"

  elements_ratios:
    $$inherit: "/properties/optimade/structures/elements_ratios"
    x-optimade-requirements:
      support: "should"
      sortable: false
      query-support: "all mandatory"
      response-level: "may"

  chemical_formula_descriptive:
    $$inherit: "/properties/optimade/structures/chemical_formula_descriptive"
    x-optimade-requirements:
      support: "should"
      sortable: false
      query-support: "all mandatory"
      response-level: "may"

  chemical_formula_reduced:
    $$inherit: "/properties/optimade/structures/chemical_formula_reduced"
    x-optimade-requirements:
      support: "should"
      sortable: false
      query-support: "equality only"
      response-level: "may"
      $comment: "If the database store chemical formulas on another format, it may not be possible to search efficiently for anything except equality."

  chemical_formula_hill:
    $$inherit: "/properties/optimade/structures/chemical_formula_hill"
    x-optimade-requirements:
      support: "may"
      sortable: false
      response-level: "may"
      query-support: "none"

  chemical_formula_anonymous:
    $$inherit: "/properties/optimade/structures/chemical_formula_anonymous"
    x-optimade-requirements:
      support: "should"
      sortable: false
      query-support: "equality only"
      response-level: "may"
      $comment: "If the database store chemical formulas on another format, it may not be possible to search efficiently for anything except equality."

  dimension_types:
    $$inherit: "/properties/optimade/structures/dimension_types"
    x-optimade-requirements:
      support: "should"
      sortable: false
      response-level: "may"
      query-support: "none"

  nperiodic_dimensions:
    $$inherit: "/properties/optimade/structures/nperiodic_dimensions"
    x-optimade-requirements:
      support: "should"
      sortable: false
      response-level: "may"
      query-support: "all mandatory"

  lattice_vectors:
    $$inherit: "/properties/optimade/structures/lattice_vectors"
    x-optimade-requirements:
      support: "should"
      sortable: false
      response-level: "may"
      query-support: "none"

  space_group_hall:
    $$inherit: "/properties/optimade/structures/space_group_hall"
    x-optimade-requirements:
      support: "may"
      sortable: false
      response-level: "may"
      query-support: "none"

  space_group_it_number:
    $$inherit: "/properties/optimade/structures/space_group_it_number"
    x-optimade-requirements:
      support: "may"
      sortable: false
      response-level: "may"
      query-support: "none"

  cartesian_site_positions:
    $$inherit: "/properties/optimade/structures/cartesian_site_positions"
    x-optimade-requirements:
      support: "should"
      sortable: false
      response-level: "may"
      query-support: "none"

  nsites:
    $$inherit: "/properties/optimade/structures/nsites"
    x-optimade-requirements:
      support: "should"
      sortable: false
      response-level: "may"
      query-support: "all mandatory"

  species_at_sites:
    $$inherit: "/properties/optimade/structures/species_at_sites"
    x-optimade-requirements:
      support: "should"
      sortable: false
      response-level: "may"
      query-support: "none"

  species:
    $$inherit: "/properties/optimade/structures/species"
    x-optimade-requirements:
      support: "should"
      sortable: false
      response-level: "may"
      query-support: "none"

  assemblies:
    $$inherit: "/properties/optimade/structures/assemblies"
    x-optimade-requirements:
      support: "may"
      sortable: false
      response-level: "may"
      query-support: "none"

  structure_features:
    $$inherit: "/properties/optimade/structures/structure_features"
    x-optimade-requirements:
      support: "must"
      sortable: false
      response-level: "may"
      query-support: "all mandatory"
