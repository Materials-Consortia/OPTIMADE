$id: "https://schemas.optimade.org/standards/v1.2.0/optimade"

structures:
  x-propdefs-inherit-ref: "/types/v1.2.0/optimade/structures"

  id:
    x-optimade-requirements:
      support: "must"
      sortable: true
      query-support: "all mandatory"
      response-default-level: "must"

  type:
    x-optimade-requirements:
      support: "must"
      sortable: true
      query-support: "all mandatory"
      response-default-level: "must"

  immutable_id:
    x-optimade-requirements:
      support: "may"
      sortable: false
      query-support: "all mandatory"

  last_modified:
    x-optimade-requirements:
      support: "must"
      sortable: true
      query-support: "all mandatory"
      response-default-level: "must"

  elements:
    x-optimade-requirements:
      support: "should"
      sortable: false
      query-support: "all mandatory"

  nelements:
    x-optimade-requirements:
      support: "should"
      sortable: false
      query-support: "all mandatory"

  elements_ratios:
    x-optimade-requirements:
      support: "should"
      sortable: false
      query-support: "all mandatory"

  chemical_formula_descriptive:
    x-optimade-requirements:
      support: "should"
      sortable: false
      query-support: "all mandatory"

  chemical_formula_reduced:
    x-optimade-requirements:
      support: "should"
      sortable: false
      query-support: "equality only"

  chemical_formula_hill:
    x-optimade-requirements:
      support: "may"
      sortable: false
      query-support: "none"

  chemical_formula_anonymous:
    x-optimade-requirements:
      support: "should"
      sortable: false
      query-support: "all mandatory"

  dimension_types:
    x-optimade-requirements:
      support: "should"
      sortable: false
      query-support: "none"

  nperiodic_dimensions:
    x-optimade-requirements:
      support: "should"
      sortable: false
      query-support: "all mandatory"

  lattice_vectors:
    x-optimade-requirements:
      support: "should"
      sortable: false
      query-support: "none"

  space_group_hall:
    x-optimade-requirements:
      support: "may"
      sortable: false
      query-support: "none"

  space_group_it_number:
    x-optimade-requirements:
      support: "may"
      sortable: false
      query-support: "none"

  cartesian_site_positions:
    x-optimade-requirements:
      support: "should"
      sortable: false
      query-support: "none"

  nsites:
    x-optimade-requirements:
      support: "should"
      sortable: false
      query-support: "all mandatory"

  species_at_sites:
    x-optimade-requirements:
      support: "should"
      sortable: false
      query-support: "none"

  species:
    x-optimade-requirements:
      support: "should"
      sortable: false
      query-support: "none"

  assemblies:
    x-optimade-requirements:
      support: "should"
      sortable: false
      query-support: "none"

  structure_features:
    x-optimade-requirements:
      support: "must"
      sortable: true
      query-support: "all mandatory"

files:
  x-propdefs-inherit-ref: "/types/v1.2.0/optimade/files"

  url:
    x-optimade-requirements:
      support: "must"
      sortable: false
      query-support: "none"
      response-default-level: "must"

  url_stable_until:
    x-optimade-requirements:
      support: "may"
      sortable: false
      query-support: "none"

  name:
    x-optimade-requirements:
      support: "may"
      sortable: false
      query-support: "none"

  size:
    x-optimade-requirements:
      support: "may"
      sortable: false
      query-support: "none"

  media_type:
    x-optimade-requirements:
      support: "may"
      sortable: false
      query-support: "none"

  version:
    x-optimade-requirements:
      support: "may"
      sortable: false
      query-support: "none"

  modification_timestamp:
    x-optimade-requirements:
      support: "may"
      sortable: false
      query-support: "none"
