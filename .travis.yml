sudo: required

dist: xenial

addons:
    apt:
        packages:
            - ant
            - ant-optional
            - unzip
	    - wget
	    - make
            - default-jdk
            - subversion

script:
    - make --version
    - make audit
