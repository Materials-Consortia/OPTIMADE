sudo: required

dist: xenial

addons:
    apt:
        packages:
            - ant
            - ant-optional
            - unzip
            - wget
            - make
            - openjdk-8-jdk
            - subversion

script:
    - make --version
    - make audit
