#---*- Makefile -*-------------------------------------------------------

# Configure where the testing tools reside. The ${TOOL_DIR} variable,
# as of 2018-11-20, is used in Makelocal-grammars, Makelocal-tests and
# Makelocal-tools makefiles:

TOOL_DIR = tests/tools
